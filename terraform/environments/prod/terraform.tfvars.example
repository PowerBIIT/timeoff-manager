# Production Environment Variables
# Skopiuj do terraform.tfvars i uzupełnij SILNYMI wartościami

db_admin_username = "dbadmin"

# WAŻNE: Użyj silnego hasła minimum 16 znaków
# Generuj: openssl rand -base64 24
db_admin_password = "CHANGE-THIS-STRONG-PASSWORD-MIN-16-CHARS!"

# WAŻNE: Użyj unikalnego klucza minimum 32 znaki (hex)
# Generuj: openssl rand -hex 32
secret_key = "CHANGE-THIS-UNIQUE-SECRET-KEY-MIN-32-CHARS"
